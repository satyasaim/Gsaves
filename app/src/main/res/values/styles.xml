<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <!-- Base application theme. -->
    <style name="GsavesTheme" parent="Theme.AppCompat.Light.NoActionBar">

        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
       <item name="colorPrimaryDark">@color/Gsavesheader</item>
        <!--<item name="colorPrimaryDark">@color/transparent</item>-->
        <item name="colorAccent">@color/colorAccent</item>

    </style>

    <style name="GsavesTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="GsavesTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="MyTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:windowNoTitle">true</item>
    </style>
    <style name="NavigationTheme" parent="AppTheme">
        <item name="listPreferredItemHeightSmall">25dp</item><!-- menu item height- vary as u need -->
    </style>

</resources>
